<div class="col-md-4">
  <div class="list-group">
    {{#eachKeyValue mitDays}}
      {{#link-to 'mits.filtered' key classNames='list-group-item'}}
        {{key}}
        <span class="badge">{{value.length}}</span>
      {{/link-to}}
    {{/eachKeyValue}}
 </div>
</div>

<div class="col-md-8">
  {{outlet}}
</div>

