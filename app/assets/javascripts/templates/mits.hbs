<div class="col-md-4">
  <div class="list-group">
  {{#each mitDays}}
    {{#link-to 'mits.index' date=this.[0] classNames='list-group-item'}}
      {{this.[0]}}
      <span class="badge">{{this.[1]}}</span>
    {{/link-to}}
  {{/each}}

 </div>
</div>

<div class="col-md-8">
  {{outlet}}
</div>

